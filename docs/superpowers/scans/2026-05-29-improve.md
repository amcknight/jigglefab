---
date: 2026-05-29
focus: "full sweep"
git_head: 422e363
lenses_run: [architect, control-inversion, test-skeptic, dead-code, types, observability]
critiques_run: [skeptic, convergence]
raw_findings: 41
merged_findings: 22
picked: 1
status: full_scan
---

## Top wins

### must-fix
- (none) — verification killed every crash claim; editor invariants hold.

### high-leverage
- Bond-representation unification — `fab.rs:19` / `editor.rs:110,161` / `gpu/scheduler.rs:41` — one `BondPair`/`BondSet` newtype owning canonical ordering collapses 4 representations + 3 conversion sites; timely because save/load adds a 5th — size: medium

### convergent win
- (none strong enough to claim without padding)

### nearby cleanup
- `scripts/verify-web.py:194` — hardcoded 300ms sleep before revert assertion → poll a tick-advanced predicate; `:78` timeout arithmetic can go negative — size: trivial (lens-reported, not re-read)
- `gpu/scheduler.rs:16,49` — drop dead `_chemistry_table` field (uploaded then never read) + unused `_frame_dt` param — size: trivial (verified)
- `app.rs:783/788/801` — render-loop unwraps could be `debug_assert!` + graceful skip for WASM robustness — size: trivial (verified safe-but-fragile)
- `editor.rs:100-116` — all `Scene` fields `pub`; intentional for single-author, revisit only if invariant-coordination becomes painful — size: medium

## Picked this session
- Bond-representation unification → handed off to: writing-plans → branch: improve/bond-representation

## Dropped during critique
- Item 3 (stale selection/bead index → panic) — KILLED by skeptic + verify: `delete_selection` clears the selection set (editor.rs:328) and remaps surviving bonds, so stale indices cannot reach `translate_selection`/`hit_selected`.
- Item 10 (bit-exact `to_bits()` equality in parallel tests "flaky across SIMD") — KILLED: determinism is an explicit P1 goal; the seq==parallel bit-exact assertion is the intended invariant. Epsilon comparison would *weaken* the test.
- Item 19 (palette `zip` silent truncation) — KILLED: states/colors length mismatch is rejected at chemistry-load time (`rejects_colors_count_mismatch`, chemistry/mod.rs), so the render-path zip can never see a mismatch.
- Item 2 panic claim (chemistry-switch → out-of-range `next_state_idx` → panic at `place()` editor.rs:201/224) — DEBUNKED: `next_state_idx` is bounds-checked before assignment (app.rs:736) and reset to 0 by `switch_chemistry`, which also empties beads (editor.rs:358-364). Residual `unwrap_or(0)` silent-remap is near-unreachable defensive code (downgraded to low-value nearby cleanup).
- Skeptic MISSED item (JS `set_edit_state(idx)` unvalidated → panic) — DEBUNKED: guard exists at app.rs:736 (`if (idx as usize) < scene.chemistry.states.len()`).
- Item 21 (chemistry compile failure leaves half-built Sim) — DOWNGRADED: `to_sim()` runs before compile and the method early-returns on failure, so `self.sim` is never left half-built.
- Item 22 (set_proxy WASM ordering coupling) — DOWNGRADED: necessary single-threaded WASM init invariant, not a bug.
- Item 5 (scheduler `&mut Sim` + BeadPool shadow copy) — DOWNGRADED: documented Phase-1 limitation; births/deaths (which would exercise the divergence) are unimplemented. `sim_to_slot` currently unused but kept for that future.
- Items 13/14/16/20 — DOWNGRADED to acceptable-for-prototype (silent JS interop on a demo boundary; single-slot command struct is fine; stringly-typed snapshot has working converters; hardcoded native scenario is a known limitation).

## New finds from VERIFY pass
- Confirmed the WIP `src/fab.rs` change (uncommitted at scan time) is save/load groundwork: adds `Serialize` derives + `skip_serializing_if` + `fab_serializes_and_reparses` round-trip test. Constrains the bond refactor: any newtype MUST serialize as `bonds = [[0, 1]]` (`Vec<[u32;2]>` TOML form) to keep that test green.

## Patterns observed by lenses
- (architect) Encapsulation is permissive by design (Scene fields pub); scheduler multiplicity (sequential / parallel / mt / gpu) is justified asymmetry — different execution models, not duplication. All reachable via scheduler_selector.
- (control-inversion) Edit vs Run is an asymmetric two-representation model (Scene authoritative in Edit, Sim in Run); renderer + app branch on mode. A `Drawable`/`SimView` trait could unify but it's only 2 cases — low ROI.
- (test-skeptic) Physics tests pin to the Haskell oracle on purpose (not brittle). Bit-exact parallel==sequential tests are determinism guarantees. verify-web.py is long/stateful with one hardcoded sleep.
- (dead-code) Codebase is clean; only the two underscore-marked GPU fields are genuinely dead. All binaries + schedulers wired and reachable.
- (types) Bond representation inconsistency is the most pervasive type issue; bead/state indices travel as bare u32 (SoA is an intentional perf choice, not a smell).
- (observability) Calibrated for a research toy; the real lens-6 value was probing unwrap/panic reachability — all turned out guarded.

## Full merged list (collapsed)
1. Bond representation inconsistency — fab.rs:19 / editor.rs:110,139,161 / gpu/scheduler.rs:41 / parallel schedulers [types, architect] — PICKED
2. Stale state-index after chemistry switch — editor.rs:201/224, app.rs:807 [observability] — debunked panic / low-value residual
3. Stale selection indices → panic — editor.rs translate/hit/chain [observability] — KILLED
4. Render-loop unwraps — app.rs:783/788/801/378 [observability] — nearby cleanup (trivial)
5. Scheduler &mut Sim + BeadPool shadow — parallel/scheduler.rs:13-86 [control-inversion] — downgraded
6. Render branches Edit/Run; Drawable trait — app.rs:781-818 [architect, control-inversion] — low ROI
7. transition_mode juggles 6 fields — app.rs:526-552 [control-inversion] — fragile-but-safe
8. Scene fields all pub — editor.rs:100-116 [architect] — nearby cleanup (intentional)
9. Dead GPU fields _chemistry_table/_frame_dt — gpu/scheduler.rs:16,49 [dead-code] — nearby cleanup (trivial, verified)
10. Bit-exact float tests — substep.rs:646-703,708-786 [test-skeptic] — KILLED
11. Timing perf assertion — sim.rs:515-531 [test-skeptic] — already gated release-only
12. verify-web.py brittleness — :194 sleep, :78 arithmetic [test-skeptic] — nearby cleanup (trivial)
13. Silent JS interop — app.rs:115/187/196/674 [observability] — downgraded
14. Web commands drained on redraw — app.rs:725-776 [architect, control-inversion] — single-slot struct, fine
15. Chemistry registry split — editor.rs:85-96 vs app.rs presets [architect] — organizational only
16. Stringly-typed mode/tool snapshot — app.rs:40,44 [types] — converters exist, minor
17. Stringly-typed chemistry action/opcode — chemistry/mod.rs:53, compiler.rs:74-192 [types] — TOML boundary, validated at parse
18. BeadSpec state: String vs Sim u32 — fab.rs:30 [types] — TOML serialization form
19. Palette zip truncation — app.rs:829-831 [types] — KILLED
20. Native hardcodes scenario — app.rs:581-591 [control-inversion] — known limitation
21. Chemistry compile failure / half-built Sim — app.rs:646-648,377 [control-inversion, observability] — debunked
22. set_proxy WASM ordering — app.rs:355,667 [control-inversion] — necessary invariant
